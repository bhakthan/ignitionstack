# Docker Compose — {{ project_name }} (local mode)
# Generated by IgnitionStack

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: {{ project_name | replace("-", "_") }}
      POSTGRES_USER: app
      POSTGRES_PASSWORD: localdev123
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build: ./app/backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://app:localdev123@db:5432/{{ project_name | replace("-", "_") }}
      REDIS_URL: redis://redis:6379
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

volumes:
  pgdata:
